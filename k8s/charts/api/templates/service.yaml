apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: dev
spec:
  selector:
    app: api
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
  type: { { .Values.service.type } }
